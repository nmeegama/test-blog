/**
 * @file
 * The Blog behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.theBlog = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
